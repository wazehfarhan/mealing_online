# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=meal_system

# Environment (development|production)
ENVIRONMENT=development

# Email (for notifications - configure later)
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=mealsystem@example.com

